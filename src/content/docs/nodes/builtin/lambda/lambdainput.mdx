---
title: "Lambda Input"
description: "Define the inputs for the lambda workflow."
sidebar:
  label: "Lambda Input"
---

# Lambda Input

Define the inputs for the lambda workflow.

## What it does

Defines the input parameters for a lambda workflow, creating a reusable workflow interface.

## When to use it

Use this node as the entry point of a lambda workflow to specify what data it accepts.

## Inputs and settings

| Setting | Notes |
| --- | --- |
| Inputs | Source-backed field from the node schema. |

## Outputs

Passes data into or out of a lambda workflow, or exposes a lambda workflow as a reusable node.

## Dependencies and credentials

- No explicit credential or node dependency is declared in the node description.

## Example workflow

Connect data into Lambda Input, configure the settings above, and use the output in the next workflow step.

## Troubleshooting

- Check that required settings are present before running the node.
- If the node uses browser page data, run it on the target tab after the page has loaded.
- If it calls an external service, verify credentials, permissions, and rate limits.
- This node has source tests; use them as the reference for edge-case behavior during maintenance.

## Related nodes

- [Node types overview](/nodes/builtin/node-types/)
