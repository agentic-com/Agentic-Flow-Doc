---
title: "Pick Field"
description: "Pick a specific field from the input data"
sidebar:
  label: "Pick Field"
---

# Pick Field

Pick a specific field from the input data

## What it does

Extracts a single specified field from input data, discarding all other fields.

## When to use it

Use this node to simplify data by keeping only the field you need for downstream processing.

## Inputs and settings

| Setting | Notes |
| --- | --- |
| Output field name | Source-backed field from the node schema. |

## Outputs

Returns transformed data for downstream nodes.

## Dependencies and credentials

- No explicit credential or node dependency is declared in the node description.

## Example workflow

Connect data into Pick Field, configure the settings above, and use the output in the next workflow step.

## Troubleshooting

- Check that required settings are present before running the node.
- If the node uses browser page data, run it on the target tab after the page has loaded.
- If it calls an external service, verify credentials, permissions, and rate limits.
- This node has source tests; use them as the reference for edge-case behavior during maintenance.

## Related nodes

- [Node types overview](/nodes/builtin/node-types/)
