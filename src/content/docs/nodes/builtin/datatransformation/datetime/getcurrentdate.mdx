---
title: "Get Current Date"
description: "Get the current date"
sidebar:
  label: "Get Current Date"
---

# Get Current Date

Get the current date

## What it does

Returns the current date and time as a timestamp.

## When to use it

Use this node to add timestamps to data, calculate relative dates, or log when workflows run.

## Inputs and settings

| Setting | Notes |
| --- | --- |
| Output field name | Source-backed field from the node schema. |
| Include input fields | Source-backed field from the node schema. |
| Options | Source-backed field from the node schema. |

## Outputs

Returns transformed data for downstream nodes.

## Dependencies and credentials

- No explicit credential or node dependency is declared in the node description.

## Example workflow

Connect data into Get Current Date, configure the settings above, and use the output in the next workflow step.

## Troubleshooting

- Check that required settings are present before running the node.
- If the node uses browser page data, run it on the target tab after the page has loaded.
- If it calls an external service, verify credentials, permissions, and rate limits.
- No dedicated source test was found next to this node; verify behavior manually when changing this page.

## Related nodes

- [Node types overview](/nodes/builtin/node-types/)
