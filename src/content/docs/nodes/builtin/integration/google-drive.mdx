---
title: "Google Drive"
description: "Integrate with Google Drive to manage files and folders."
sidebar:
  label: "Google Drive"
---

# Google Drive

Integrate with Google Drive to manage files and folders.

## What it does

Manages files and folders in Google Drive, including upload, download, search, and organization operations.

## When to use it

Use this node to automate file storage, backup workflows, or integrate document management into your processes.

## Inputs and settings

| Setting | Notes |
| --- | --- |
| Resource | Source-backed field from the node schema. |
| Operation | Source-backed field from the node schema. |
| File ID | Source-backed field from the node schema. |
| Folder ID | Source-backed field from the node schema. |
| Name | Source-backed field from the node schema. |
| Search Query | Source-backed field from the node schema. |
| Parent ID | Source-backed field from the node schema. |
| MIME Type | Source-backed field from the node schema. |
| Content | Source-backed field from the node schema. |
| Page Size | Source-backed field from the node schema. |

## Outputs

Returns the service response or a success object from the selected operation.

## Dependencies and credentials

- Credential type: OAuth2.

## Example workflow

Connect prepared data into Google Drive, select the credential, choose the operation, then map previous node values into the operation fields.

## Troubleshooting

- Check that required settings are present before running the node.
- If the node uses browser page data, run it on the target tab after the page has loaded.
- If it calls an external service, verify credentials, permissions, and rate limits.
- No dedicated source test was found next to this node; verify behavior manually when changing this page.

## Related nodes

- [Http Request](/nodes/builtin/core/http-request/)
- [Edit Fields](/nodes/builtin/datatransformation/editfields/)
