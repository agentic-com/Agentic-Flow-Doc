---
title: "Edit Fields"
description: "Modify, add or remove item fields"
sidebar:
  label: "Edit Fields"
---

# Edit Fields

Modify, add or remove item fields

## What it does

Edit Fields belongs to the **Data Transformation** palette group. Its canonical node id is `editFields` and its source documentation link is `/nodes/builtin/datatransformation/editfields/`.

## When to use it

Use Edit Fields to reshape workflow data before sending it to another node.

## Inputs and settings

| Setting | Notes |
| --- | --- |
| fields | Source-backed field from the node schema. |

## Outputs

Returns transformed data for downstream nodes.

## Dependencies and credentials

- No explicit credential or node dependency is declared in the node description.

## Example workflow

Connect data into Edit Fields, configure the settings above, and use the output in the next workflow step.

## Troubleshooting

- Check that required settings are present before running the node.
- If the node uses browser page data, run it on the target tab after the page has loaded.
- If it calls an external service, verify credentials, permissions, and rate limits.
- This node has source tests; use them as the reference for edge-case behavior during maintenance.

## Related nodes

- [Node types overview](/nodes/builtin/node-types/)
