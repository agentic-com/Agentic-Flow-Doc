---
title: "Get HTML from Link"
description: "Get the HTML content from a given URL"
sidebar:
  label: "Get HTML from Link"
---

# Get HTML from Link

Get the HTML content from a given URL

## What it does

Get HTML from Link belongs to the **Core** palette group. Its canonical node id is `getHTMLFromLinkNode` and its source documentation link is `/nodes/builtin/core/gethtmlfromlink/`.

## When to use it

Use Get HTML from Link for generic workflow operations such as requests, code, or URL content retrieval.

## Inputs and settings

| Setting | Notes |
| --- | --- |
| URL | Source-backed field from the node schema. |
| Output field name | Source-backed field from the node schema. |
| Timeout | Source-backed field from the node schema. |
| Headers | Source-backed field from the node schema. |
| Include input fields | Source-backed field from the node schema. |
| Options | Source-backed field from the node schema. |

## Outputs

Returns node-specific output described by the implementation and visible in workflow execution data.

## Dependencies and credentials

- No explicit credential or node dependency is declared in the node description.

## Example workflow

Connect data into Get HTML from Link, configure the settings above, and use the output in the next workflow step.

## Troubleshooting

- Check that required settings are present before running the node.
- If the node uses browser page data, run it on the target tab after the page has loaded.
- If it calls an external service, verify credentials, permissions, and rate limits.
- This node has source tests; use them as the reference for edge-case behavior during maintenance.

## Related nodes

- [Code](/nodes/builtin/core/code/)
- [Http Request](/nodes/builtin/core/http-request/)
- [Edit Fields](/nodes/builtin/datatransformation/editfields/)
