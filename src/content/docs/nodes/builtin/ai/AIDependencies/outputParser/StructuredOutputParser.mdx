---
title: "Structured Output Parser"
description: "Return data in a defined JSON format"
sidebar:
  label: "Structured Output Parser"
---

# Structured Output Parser

Return data in a defined JSON format

## What it does

Structured Output Parser belongs to the **Output Parser** palette group. Its canonical node id is `structuredOutputParser` and its source documentation link is `/nodes/builtin/ai/aidependencies/outputparser/structuredoutputparser/`.

## When to use it

Use Structured Output Parser as part of an AI workflow, either as an agent node or as a dependency connected to an agent.

## Inputs and settings

| Setting | Notes |
| --- | --- |
| JSON Output Example | Source-backed field from the node schema. |

## Outputs

Returns an output parser dependency used to structure model responses.

## Dependencies and credentials

- No explicit credential or node dependency is declared in the node description.

## Example workflow

Connect Structured Output Parser to an AI agent or dependency input that accepts this dependency type, then run the agent with data from previous nodes.

## Troubleshooting

- Check that required settings are present before running the node.
- If the node uses browser page data, run it on the target tab after the page has loaded.
- If it calls an external service, verify credentials, permissions, and rate limits.
- No dedicated source test was found next to this node; verify behavior manually when changing this page.

## Related nodes

- [AI Agents](/nodes/builtin/ai/aiagents/basicllmchainnode/)
- [Chat OpenAI](/nodes/builtin/ai/aidependencies/llm/openai/)
- [Local Knowledge](/nodes/builtin/ai/aidependencies/vectorstore/localknowledge/)
