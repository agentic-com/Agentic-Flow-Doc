---
title: "Chrome AI"
description: "Leverage Chrome AI to use LLM directly on your device"
sidebar:
  label: "Chrome AI"
---

# Chrome AI

Leverage Chrome AI to use LLM directly on your device

## What it does

Chrome AI belongs to the **Chat LLM** palette group. Its canonical node id is `chrome-ai` and its source documentation link is `/nodes/builtin/ai/aidependencies/llm/chrome-ai/`.

## When to use it

Use Chrome AI as part of an AI workflow, either as an agent node or as a dependency connected to an agent.

## Inputs and settings

| Setting | Notes |
| --- | --- |
| Prompt | Source-backed field from the node schema. |
| Temperature | Source-backed field from the node schema. |
| Max Retries | Source-backed field from the node schema. |
| Options | Source-backed field from the node schema. |

## Outputs

Returns a chat model dependency that can be connected to AI agent nodes.

## Dependencies and credentials

- No explicit credential or node dependency is declared in the node description.

## Example workflow

Connect Chrome AI to an AI agent or dependency input that accepts this dependency type, then run the agent with data from previous nodes.

## Troubleshooting

- Check that required settings are present before running the node.
- If the node uses browser page data, run it on the target tab after the page has loaded.
- If it calls an external service, verify credentials, permissions, and rate limits.
- No dedicated source test was found next to this node; verify behavior manually when changing this page.

## Related nodes

- [AI Agents](/nodes/builtin/ai/aiagents/basicllmchainnode/)
- [Chat OpenAI](/nodes/builtin/ai/aidependencies/llm/openai/)
- [Local Knowledge](/nodes/builtin/ai/aidependencies/vectorstore/localknowledge/)
