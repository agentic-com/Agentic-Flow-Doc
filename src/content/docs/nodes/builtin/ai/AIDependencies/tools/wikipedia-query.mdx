---
title: "Wikipedia Query"
description: "Query Wikipedia for information on a given topic."
sidebar:
  label: "Wikipedia Query"
---

# Wikipedia Query

Query Wikipedia for information on a given topic.

## What it does

Wikipedia Query belongs to the **Tools** palette group. Its canonical node id is `wikipedia-query` and its source documentation link is `/nodes/builtin/ai/aidependencies/tools/wikipedia-query/`.

## When to use it

Use Wikipedia Query as part of an AI workflow, either as an agent node or as a dependency connected to an agent.

## Inputs and settings

| Setting | Notes |
| --- | --- |
| Top K Results | Source-backed field from the node schema. |
| Max Document Content Length | Source-backed field from the node schema. |

## Outputs

Returns a tool dependency that agent nodes can call.

## Dependencies and credentials

- No explicit credential or node dependency is declared in the node description.

## Example workflow

Connect Wikipedia Query to an AI agent or dependency input that accepts this dependency type, then run the agent with data from previous nodes.

## Troubleshooting

- Check that required settings are present before running the node.
- If the node uses browser page data, run it on the target tab after the page has loaded.
- If it calls an external service, verify credentials, permissions, and rate limits.
- No dedicated source test was found next to this node; verify behavior manually when changing this page.

## Related nodes

- [AI Agents](/nodes/builtin/ai/aiagents/basicllmchainnode/)
- [Chat OpenAI](/nodes/builtin/ai/aidependencies/llm/openai/)
- [Local Knowledge](/nodes/builtin/ai/aidependencies/vectorstore/localknowledge/)
