---
title: "Character Text Splitter"
description: "Breaks down text into smaller parts."
sidebar:
  label: "Character Text Splitter"
---

# Character Text Splitter

Breaks down text into smaller parts.

## What it does

Splits text into fixed-size chunks based on character count.

## When to use it

Connect this to indexer nodes when you need simple, predictable text chunking for document processing.

## Inputs and settings

| Setting | Notes |
| --- | --- |
| Chunk Size | Source-backed field from the node schema. |
| Separator | Source-backed field from the node schema. |
| Chunk Overlap | Source-backed field from the node schema. |

## Outputs

Returns a text splitter dependency used before indexing or retrieval.

## Dependencies and credentials

- No explicit credential or node dependency is declared in the node description.

## Example workflow

Connect Character Text Splitter to an AI agent or dependency input that accepts this dependency type, then run the agent with data from previous nodes.

## Troubleshooting

- Check that required settings are present before running the node.
- If the node uses browser page data, run it on the target tab after the page has loaded.
- If it calls an external service, verify credentials, permissions, and rate limits.
- This node has source tests; use them as the reference for edge-case behavior during maintenance.

## Related nodes

- [AI Agents](/nodes/builtin/ai/aiagents/basicllmchainnode/)
- [Chat OpenAI](/nodes/builtin/ai/aidependencies/llm/openai/)
- [Local Knowledge](/nodes/builtin/ai/aidependencies/vectorstore/localknowledge/)
