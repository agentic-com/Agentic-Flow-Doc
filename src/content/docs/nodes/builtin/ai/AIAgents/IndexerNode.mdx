---
title: "Indexer"
description: "Splitting text into chunks and embedding them into a vector store"
sidebar:
  label: "Indexer"
---

# Indexer

Splitting text into chunks and embedding them into a vector store

## What it does

Indexer belongs to the **AI Agents** palette group. Its canonical node id is `indexer` and its source documentation link is `/nodes/builtin/ai/aiagents/indexernode/`.

## When to use it

Use Indexer as part of an AI workflow, either as an agent node or as a dependency connected to an agent.

## Inputs and settings

| Setting | Notes |
| --- | --- |
| Input | Source-backed field from the node schema. |

## Outputs

Returns node-specific output described by the implementation and visible in workflow execution data.

## Dependencies and credentials

- No explicit credential or node dependency is declared in the node description.

## Example workflow

Connect Indexer to an AI agent or dependency input that accepts this dependency type, then run the agent with data from previous nodes.

## Troubleshooting

- Check that required settings are present before running the node.
- If the node uses browser page data, run it on the target tab after the page has loaded.
- If it calls an external service, verify credentials, permissions, and rate limits.
- This node has source tests; use them as the reference for edge-case behavior during maintenance.

## Related nodes

- [AI Agents](/nodes/builtin/ai/aiagents/basicllmchainnode/)
- [Chat OpenAI](/nodes/builtin/ai/aidependencies/llm/openai/)
- [Local Knowledge](/nodes/builtin/ai/aidependencies/vectorstore/localknowledge/)
