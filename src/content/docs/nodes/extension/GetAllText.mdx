---
title: "Get All Text"
description: "Get the selected text from the page of the actual openend tab"
sidebar:
  label: "Get All Text"
---

# Get All Text

Get the selected text from the page of the actual openend tab

## What it does

Extracts all visible text content from the currently active browser tab.

## When to use it

Use this node to scrape text from web pages for analysis, indexing, or AI processing.

## Inputs and settings

| Setting | Notes |
| --- | --- |
| Output field name | Source-backed field from the node schema. |
| Include input fields | Source-backed field from the node schema. |
| Visible text only | Source-backed field from the node schema. |
| Options | Source-backed field from the node schema. |

## Outputs

Returns the extracted page data or the result of the page interaction.

## Dependencies and credentials

- No explicit credential or node dependency is declared in the node description.

## Example workflow

Run Get All Text after opening the target page, then pass its output to Edit Fields, an AI node, or an integration node.

## Troubleshooting

- Check that required settings are present before running the node.
- If the node uses browser page data, run it on the target tab after the page has loaded.
- If it calls an external service, verify credentials, permissions, and rate limits.
- This node has source tests; use them as the reference for edge-case behavior during maintenance.

## Related nodes

- [Edit Fields](/nodes/builtin/datatransformation/editfields/)
- [Http Request](/nodes/builtin/core/http-request/)
