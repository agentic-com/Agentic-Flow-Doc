---
title: "Highlight Elements"
description: "Highlight selected elements in the opened page"
sidebar:
  label: "Highlight Elements"
---

# Highlight Elements

Highlight selected elements in the opened page

## What it does

Visually highlights selected elements on the page with colored borders or overlays.

## When to use it

Use this node to draw attention to specific elements, guide users, or debug element selection.

## Inputs and settings

| Setting | Notes |
| --- | --- |
| Target Element | Source-backed field from the node schema. |
| Elements | Source-backed field from the node schema. |
| Label | Source-backed field from the node schema. |
| Duration | Source-backed field from the node schema. |

## Outputs

Returns the extracted page data or the result of the page interaction.

## Dependencies and credentials

- No explicit credential or node dependency is declared in the node description.

## Example workflow

Run Highlight Elements after opening the target page, then pass its output to Edit Fields, an AI node, or an integration node.

## Troubleshooting

- Check that required settings are present before running the node.
- If the node uses browser page data, run it on the target tab after the page has loaded.
- If it calls an external service, verify credentials, permissions, and rate limits.
- This node has source tests; use them as the reference for edge-case behavior during maintenance.

## Related nodes

- [Edit Fields](/nodes/builtin/datatransformation/editfields/)
- [Http Request](/nodes/builtin/core/http-request/)
