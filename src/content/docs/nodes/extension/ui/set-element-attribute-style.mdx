---
title: "Set Element Attribute Style"
description: "Set attributes and styles on selected elements in the opened page"
sidebar:
  label: "Set Element Attribute Style"
---

# Set Element Attribute Style

Set attributes and styles on selected elements in the opened page

## What it does

Modifies HTML attributes and CSS styles of selected elements on the page.

## When to use it

Use this node to customize page appearance, modify element behavior, or apply dynamic styling.

## Inputs and settings

| Setting | Notes |
| --- | --- |
| Name | Source-backed field from the node schema. |
| Value | Source-backed field from the node schema. |
| Remove | Source-backed field from the node schema. |
| Attributes | Source-backed field from the node schema. |
| CSS property | Source-backed field from the node schema. |
| Styles | Source-backed field from the node schema. |

## Outputs

Returns the extracted page data or the result of the page interaction.

## Dependencies and credentials

- No explicit credential or node dependency is declared in the node description.

## Example workflow

Run Set Element Attribute Style after opening the target page, then pass its output to Edit Fields, an AI node, or an integration node.

## Troubleshooting

- Check that required settings are present before running the node.
- If the node uses browser page data, run it on the target tab after the page has loaded.
- If it calls an external service, verify credentials, permissions, and rate limits.
- This node has source tests; use them as the reference for edge-case behavior during maintenance.

## Related nodes

- [Edit Fields](/nodes/builtin/datatransformation/editfields/)
- [Http Request](/nodes/builtin/core/http-request/)
